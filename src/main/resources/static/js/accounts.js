document.addEventListener("DOMContentLoaded",()=>{const search=document.getElementById("accountSearch");const type=document.getElementById("accountType");const rows=document.querySelectorAll(".table-row");function filter(){const text=(search?.value||"").toLowerCase();const selected=type?.value||"";rows.forEach(row=>{const content=row.innerText.toLowerCase();const matchesText=content.includes(text);const matchesType=!selected||content.includes(selected.toLowerCase());row.style.display=matchesText&&matchesType?"grid":"none";});}search?.addEventListener("input",filter);type?.addEventListener("change",filter);});